# SentinelFlow Verified Capability Matrix
#
# Single source of truth for submission-facing capability status.
# Legal status vocabulary:
#   TESTED       — Implemented and covered by automated tests in CI
#   IMPLEMENTED  — Code/schema exists but full/live integration evidence is pending
#   DEMO_ONLY    — Development or demonstration profile implementation only
#   EXPERIMENTAL — Proof-of-concept implementation
#   PLANNED      — Designed; implementation pending

version: "1.2-p11.5-p12.5"
last_updated: "2026-08-23"

capabilities:
  # --- Core Deterministic Financial Processing ---
  nacha_validation:
    status: TESTED
    evidence: gateway/internal/nacha/validate_test.go
    description: "Zero-copy NACHA parser with Mod-10 check digit and batch hash verification"
    test_command: "go test ./internal/nacha/ -run TestValidate"

  fail_closed_quarantine:
    status: TESTED
    evidence: gateway/quarantine_test.go
    description: "Malformed or unbalanced files transition to QUARANTINED immediately"
    test_command: "go test -run TestQuarantine"

  dual_control_release:
    status: TESTED
    evidence: gateway/internal/review/review_test.go
    description: "Separation-of-duties release requiring N approvals from distinct reviewers"
    test_command: "go test ./internal/review/ -run TestReview"

  linear_hash_chain:
    status: TESTED
    evidence: gateway/internal/ledger/ledger_test.go
    description: "Append-only SHA-256 linear hash chain (NOT Merkle tree, NOT digital signature)"
    test_command: "go test ./internal/ledger/ -run TestLedger"

  immutable_artifact_storage:
    status: TESTED
    evidence: gateway/internal/objectstore/conformance_test.go
    description: "Uploaded files stored as immutable blobs in S3/MinIO with SHA-256 addressability"
    test_command: "go test ./internal/objectstore/ -run TestConformance"

  # --- Security, Authentication & Isolation ---
  tenant_isolation:
    status: TESTED
    evidence: gateway/tenant_isolation_test.go
    description: "Every database query scoped by tenant_id; cross-tenant access returns 404/403"
    test_command: "go test -run TestTenantIsolation"

  tenant_scoped_ai_triage:
    status: TESTED
    evidence: gateway/triage_test.go
    description: "AI triage endpoint scoped to authenticated tenant context; cross-tenant access returns 404"
    test_command: "go test -run TestTriageTenantIsolation"

  canonical_agent_context_envelope:
    status: TESTED
    evidence: gateway/triage_test.go
    description: "Immutable typed envelope with server-injected tenant ID, budget, and zero raw financial payloads"
    test_command: "go test -run TestAgentContextEnvelopeInvariantsAndDelivery"

  bounded_ai_service_client:
    status: TESTED
    evidence: gateway/triage_test.go
    description: "Outbound AI client with explicit timeout, retry policy, response size ceiling, and header propagation"
    test_command: "go test -run TestAIClientBoundedPolicyAndTimeout"

  fail_closed_provider_semantics:
    status: TESTED
    evidence: gateway/ai_analyst_test.go
    description: "When AI tier is offline or unconfigured, returns HTTP 503 UNAVAILABLE; zero fabricated fallback claims"
    test_command: "go test -run TestAiAnalyst_Returns503WhenUnconfiguredOrOffline"

  rbac_permissions:
    status: TESTED
    evidence: gateway/router_auth_test.go
    description: "8-capability RBAC model enforced at route level"
    test_command: "go test -run TestRouterAuth"

  secret_redaction:
    status: TESTED
    evidence: gateway/internal/secrets/redact_test.go
    description: "Credentials sealed with AES-256-GCM and redacted in logs/JSON/errors"
    test_command: "go test ./internal/secrets/ -run TestRedact"

  ssrf_prevention:
    status: TESTED
    evidence: gateway/threat_model_test.go
    description: "RFC1918 and loopback ranges blocked for connector URIs"
    test_command: "go test -run TestSSRFProtection"

  # --- Gemini 3.5 & Multi-Agent Operations ---
  gemini_3_5_provider_path:
    status: TESTED
    evidence: ai-tier/tests/test_return_risk_agent.py
    description: "Governed Gemini 3.5 Flash provider path through GuardedModelBoundary; ReturnRiskAgent has no direct provider invocation path"
    test_command: "pytest ai-tier/tests/test_return_risk_agent.py -v"

  live_gemini_3_5:
    status: IMPLEMENTED
    evidence: ai-tier/guardrails/boundary.py
    description: "Gemini 3.5 Flash live invocation path is implemented; TESTED status requires separately observed external live-provider execution"

  read_only_ai_investigator:
    status: TESTED
    evidence: ai-tier/evals/runner.py
    description: "AI tier has no database write credentials, no mutation endpoints, no shell execution"
    test_command: "python ai-tier/evals/runner.py"

  agent_development_kit_orchestration:
    status: TESTED
    evidence: ai-tier/tests/test_adk_introspection.py
    description: "Real Google ADK Agent instances registered for specialized sub-agents with trust-partitioned prompts"
    test_command: "pytest ai-tier/tests/test_adk_introspection.py -v"

  durable_agent_workflows:
    status: TESTED
    evidence: gateway/internal/repository/agent_workflow_repo_test.go
    description: "Crash-resilient workflow state machine in Go gateway backed by atomic DB transitions and durable outbox events"
    test_command: "go test ./internal/repository/ -run TestAgentWorkflow"

  parallel_specialist_execution:
    status: TESTED
    evidence: ai-tier/tests/test_durable_orchestration.py
    description: "Deterministic synthesis across parallel specialists with partial failure isolation and unified budget enforcement"
    test_command: "pytest ai-tier/tests/test_durable_orchestration.py -v"

  policy_sla_agent:
    status: TESTED
    evidence: ai-tier/agents/policy_sla.py
    description: "Autonomy Level A1 specialist explaining deterministic policy decisions and contract cutoff context"
    test_command: "pytest ai-tier/tests/test_policy_sla_agent.py -v"

  toctou_and_policy_authority:
    status: TESTED
    evidence: ai-tier/tests/test_toctou_and_policy_authority.py
    description: "PolicyEngine.Evaluate strictly dominates agent opinions; TOCTOU policy bundle hash and artifact SHA changes fail closed"
    test_command: "pytest ai-tier/tests/test_toctou_and_policy_authority.py -v"

  agent_tool_gateway:
    status: TESTED
    evidence: gateway/internal/toolgateway/gateway_test.go
    description: "Exclusive gateway mediating AI tool calls with manifest enforcement, capability allowlists, rate limiting, and side-effect segregation"
    test_command: "go test ./internal/toolgateway/ -run TestToolGateway"

  remediation_agent:
    status: TESTED
    evidence: ai-tier/agents/remediation.py
    description: "Autonomy Level A2 remediation planner proposing declarative repair operations with zero raw file byte access"
    test_command: "pytest ai-tier/tests/test_remediation_agent.py -v"

  verifier_agent:
    status: TESTED
    evidence: ai-tier/agents/verifier.py
    description: "Autonomy Level A1 read-only critic reviewing structured verification evidence"
    test_command: "pytest ai-tier/tests/test_verifier_agent.py -v"

  memory_agent:
    status: TESTED
    evidence: ai-tier/agents/memory_agent.py
    description: "Autonomy Level A1 read-only memory specialist retrieving bounded advisory historical context"
    test_command: "pytest ai-tier/tests/test_memory_agent.py -v"

  agent_runtime:
    status: IMPLEMENTED
    evidence: gateway/migrations/011_agent_workflows.sql
    description: "Go-owned durable agent workflow runtime; managed Google runtime is separately statused"

  agent_registry:
    status: IMPLEMENTED
    evidence: docs/registry/agent_registry_v1.json
    description: "Declarative Google Agent Registry payload for the fixed fleet and bounded agent-tools endpoint; live registry creation remains externally unproven"

  # --- Governed Operational Memory ---
  go_operational_memory:
    status: TESTED
    evidence: gateway/internal/memory/service_test.go
    description: "Go-owned authoritative operational fact store with deterministic eligibility gate, PII rejection, RFC 8785 canonical hashing, and outbox bridge"
    test_command: "go test ./internal/memory/ -v"

  sqlite_operational_memory:
    status: TESTED
    evidence: gateway/internal/memory/service_test.go
    description: "SQLite operational-memory persistence, triggers, revisions, and concurrency isolation"
    test_command: "go test ./internal/memory/ -v"

  postgresql_operational_memory:
    status: IMPLEMENTED
    evidence: gateway/migrations/022_operational_memory.sql
    description: "PostgreSQL 16 operational-memory schema and migrations"

  memory_source_resolution:
    status: TESTED
    evidence: gateway/internal/memory/resolver_test.go
    description: "Go source resolver validates tenant, source status and hashes and exclusively mints AuthorizedEvidenceRefs"
    test_command: "go test ./internal/memory/ -run TestSourceResolver"

  memory_non_authority:
    status: TESTED
    evidence: ai-tier/evals/memory_runner.py
    description: "MemoryRecall != Evidence and SimilarityScore != Trust enforced across the fleet"
    test_command: "python ai-tier/evals/memory_runner.py"

  memory_tenant_isolation:
    status: TESTED
    evidence: gateway/internal/memory/property_test.go
    description: "Strict multi-tenant memory partitioning in Go and Python"
    test_command: "go test ./internal/memory/ -run TestProperty_TenantIsolation"

  memory_poisoning_resistance:
    status: TESTED
    evidence: ai-tier/evals/memory_runner.py
    description: "Adversarial memory poisoning cannot promote advisory memory into policy/file-release authority"
    test_command: "python ai-tier/evals/memory_runner.py"

  managed_memory_adapter:
    status: TESTED
    evidence: ai-tier/memory/provider.py
    description: "Python managed-memory adapter with deterministic ranking and bounded retrieval"
    test_command: "pytest ai-tier/tests/test_memory_provider.py -v"

  managed_memory_mock:
    status: TESTED
    evidence: ai-tier/memory/mock_provider.py
    description: "In-memory test provider with fault injection"
    test_command: "pytest ai-tier/tests/test_memory_provider.py -v"

  managed_memory_revisions:
    status: TESTED
    evidence: gateway/internal/memory/property_test.go
    description: "Managed memory revision tracking cannot mutate authoritative Go operational facts"
    test_command: "go test ./internal/memory/ -run TestProperty_RevisionMonotonicity"

  live_google_memory_bank:
    status: IMPLEMENTED
    evidence: ai-tier/memory/google_provider.py
    description: "Managed Memory Bank client implemented; external live execution is not represented as tested here"

  live_ingest_events:
    status: IMPLEMENTED
    evidence: ai-tier/memory/google_provider.py
    description: "Managed memory ingest client implemented; external live execution is not represented as tested here"

  live_memory_profiles:
    status: IMPLEMENTED
    evidence: ai-tier/memory/google_provider.py
    description: "Managed memory profile retrieval client implemented; external live execution is not represented as tested here"

  # --- Governed Remediation & Independent Verification ---
  derived_artifacts:
    status: TESTED
    evidence: gateway/internal/candidate/service_test.go
    description: "Remediation creates new artifacts linked to quarantined originals, never mutating originals"
    test_command: "go test ./internal/candidate/ -run TestCandidateService_"

  governed_remediation:
    status: TESTED
    evidence: gateway/agent_orchestrator_gateway_test.go
    description: "Exclusive Tool Gateway gate, crash consistency, orphan reconciliation, deterministic candidate keys, and immutable candidate generation"
    test_command: "go test -run TestAgentOrchestrator_ExclusiveToolGateway"

  independent_verification:
    status: TESTED
    evidence: gateway/internal/verification/service_test.go
    description: "Independent Go verification re-reads ObjectStore bytes, re-runs validation, verifies derivation hashes, and enforces deterministic dominance"
    test_command: "go test ./internal/verification/ -run TestVerification"

  verification_integrity_checks:
    status: TESTED
    evidence: gateway/internal/verification/service_test.go
    description: "Typed deterministic verification checks cover parent, candidate, derivation, validator, and policy freshness"
    test_command: "go test ./internal/verification/ -run TestVerification"

  sentinelflow_lens:
    status: TESTED
    evidence: gateway/internal/lens/service.go
    description: "Lens Lite governed analytics: typed semantic intents, tenant-scoped allowlisted query compilation, append-only Investigation Threads, provenance hashes, and advisory-only visual exploration. Full MCP transport and arbitrary BI authoring are out of scope."
    test_command: "bash scripts/verify_lens_lite.sh"

  # --- AI Guardrails ---
  model_armor_client:
    status: TESTED
    evidence: ai-tier/armor/client.py
    description: "Model Armor client with regional screening, fault injection, and fail-closed required-mode behavior"
    test_command: "pytest ai-tier/tests/test_model_armor_boundary.py -v"

  guarded_model_boundary:
    status: TESTED
    evidence: ai-tier/guardrails/boundary.py
    description: "Shared hardened model boundary with minimization, trust partitioning, pre/post screening, schema validation, grounding, and audit hashing"
    test_command: "pytest ai-tier/tests/test_model_armor_boundary.py -v"

  ai_guardrails_evals:
    status: TESTED
    evidence: ai-tier/evals/model_armor_runner.py
    description: "Adversarial Model Armor security scenarios cover injection, minimization, fail-closed outage, and leakage containment"
    test_command: "python ai-tier/evals/model_armor_runner.py"

  tool_poisoning_containment:
    status: TESTED
    evidence: ai-tier/evals/adversarial_model_armor.json
    description: "Six tool-poisoning adversarial scenarios cover descriptor injection, capability escalation, rug pulls, schema poisoning, shadowing, and output poisoning"
    test_command: "python ai-tier/evals/model_armor_runner.py"

  data_sovereignty_enforcement:
    status: TESTED
    evidence: ai-tier/evals/adversarial_sovereignty.json
    description: "Geographic data sovereignty enforcement at model and memory boundaries with SF-SAFE-007 boot invariant"
    test_command: "python ai-tier/evals/sovereignty_runner.py"

  # --- Cloud Infrastructure ---
  cloud_kms_checkpoints:
    status: IMPLEMENTED
    evidence: gateway/migrations/015_kms_checkpoints.sql
    description: "Ledger checkpoint schema exists; external KMS signing remains separately unverified"

  cloud_sql_deployment:
    status: PLANNED
    evidence: deploy/setup-gcp.sh
    description: "Cloud SQL / production persistence deployment remains deferred until live deployment phase"

  # --- P11.5 Google Enterprise Agent Platform Integration Truth Gate ---
  agent_runtime_adk_deployment:
    status: IMPLEMENTED
    evidence: ai-tier/runtime/managed_adk.py
    description: "Real vertexai.agent_engines.AdkApp factory packages the fixed seven-agent Google ADK topology; deployment command exists but live Agent Runtime creation is not yet proven"
    test_command: "pytest ai-tier/tests/test_platform_runtime.py -v"

  agent_runtime_deploy_command:
    status: IMPLEMENTED
    evidence: ai-tier/runtime/deploy_agent_runtime.py
    description: "Dry-run-by-default deployment command creates Agent Runtime with identity_type=AGENT_IDENTITY only when --execute is explicitly supplied"

  agent_identity_runtime:
    status: IMPLEMENTED
    evidence: gateway/internal/auth/iap_verifier.go
    description: "Managed ingress verifies signed X-Goog-IAP-JWT-Assertion before binding fixed-roster specialist metadata; application code no longer fabricates Google Agent Identity principals"
    test_command: "go test -v ./internal/auth/..."

  agent_gateway:
    status: IMPLEMENTED
    evidence: ai-tier/runtime/gateway_client.py
    description: "Managed egress performs bounded HTTP dispatch to the exact registered Go endpoint and relies on Google Agent Gateway/IAP for infrastructure authorization; local policy simulation is labeled separately"
    test_command: "pytest ai-tier/tests/test_platform_runtime.py -v"

  agent_gateway_default_deny:
    status: TESTED
    evidence: ai-tier/evals/platform_runner.py
    description: "SentinelFlow local defense-in-depth routing rejects unregistered destinations; this is not represented as live Google Agent Gateway proof"
    test_command: "python ai-tier/evals/platform_runner.py"

  agent_observability:
    status: TESTED
    evidence: ai-tier/observability/telemetry.py
    description: "Privacy-preserving OpenTelemetry instrumentation separates allowed trace metadata from prompt payloads; live Cloud Trace proof remains separate"
    test_command: "pytest ai-tier/tests/test_platform_runtime.py -v"

  runtime_to_go_tool_gateway:
    status: IMPLEMENTED
    evidence: gateway/internal/auth/iap_verifier.go
    description: "Managed HTTP path plus signed-IAP ingress binding implemented; live Runtime→Gateway→Go execution is not yet externally proven"
    test_command: "go test -v ./internal/auth/... && pytest ai-tier/tests/test_platform_runtime.py -v"

  live_agent_runtime:
    status: IMPLEMENTED
    evidence: ai-tier/runtime/deploy_agent_runtime.py
    description: "Live deployment path exists; capability upgrades to TESTED only after a real reasoningEngine resource is created and queried"

  live_agent_identity:
    status: IMPLEMENTED
    evidence: ai-tier/runtime/identity.py
    description: "Managed identity consumption path exists; TESTED requires observing the system-attested principal from a real Agent Runtime resource"

  live_agent_registry:
    status: IMPLEMENTED
    evidence: docs/registry/agent_registry_v1.json
    description: "Registry payload exists; TESTED requires a real Google Agent Registry resource lookup"

  live_agent_gateway:
    status: IMPLEMENTED
    evidence: deployment/terraform/agent_platform.tf
    description: "Gateway infrastructure definition and managed client path exist; TESTED requires live allowed/denied egress evidence"

  live_agent_observability:
    status: TESTED
    evidence: ai-tier/observability/telemetry.py
    description: "Production-grade OpenTelemetry Cloud Trace integration with environment-gated initialization, PII attribute redaction, W3C traceparent propagation, and canonical 5-span execution hierarchy"
    test_command: "pytest ai-tier/tests/test_observability.py -v"

  platform_adversarial_evals:
    status: TESTED
    evidence: ai-tier/evals/platform_runner.py
    description: "Platform adversarial scenarios test the local governance model, identity-spoofing resistance, direct-DB bypass, and trace sanitization; they are not live-cloud proof"
    test_command: "python ai-tier/evals/platform_runner.py"

  # --- P12.5 Governed ACH Return Intelligence Truth Gate ---
  deterministic_ach_return_intelligence:
    status: TESTED
    evidence: gateway/internal/returnrisk/p12_5_test.go
    description: "Seven-feature deterministic Go risk score, representative return-code taxonomy, explicit threshold applicability, and RFC 8785 protected assessment hashing"
    test_command: "go test ./internal/returnrisk/... -v"

  return_risk_agent:
    status: TESTED
    evidence: ai-tier/tests/test_return_risk_agent.py
    description: "A1 read-only return-risk specialist uses only GuardedModelBoundary for live inference; LIVE failure cannot silently become deterministic success"
    test_command: "pytest ai-tier/tests/test_return_risk_agent.py -v"

  return_risk_memory_integration:
    status: TESTED
    evidence: gateway/internal/returnrisk/memory.go
    description: "Verified return-pattern facts can be emitted to M1 operational memory after confirmed resolution"
    test_command: "go test ./internal/returnrisk/... -v"

  return_risk_adversarial_evals:
    status: TESTED
    evidence: ai-tier/evals/return_runner.py
    description: "20 return-risk adversarial/conformance scenarios cover score immutability, memory non-authority, and authority boundaries"
    test_command: "python ai-tier/evals/return_runner.py"

  return_risk_public_semantics_fixture:
    status: TESTED
    evidence: docs/fixtures/return_risk_semantics.json
    description: "Shared Go/Python fixture pins public threshold values, R10/R11 semantics, unauthorized-rate family, R16 threshold non-applicability, and representative-catalog scope"
    test_command: "go test ./internal/returnrisk/... -v && pytest ai-tier/tests/test_return_risk_agent.py -v"

  # --- Observability, Telemetry & CI ---
  prometheus_metrics:
    status: TESTED
    evidence: gateway/internal/telemetry/telemetry_test.go
    description: "Low-cardinality Prometheus metrics with normalized routes and status labels"
    test_command: "go test ./internal/telemetry/ -run TestTelemetry"

  adversarial_evals:
    status: TESTED
    evidence: ai-tier/evals/runner.py
    description: "Master adversarial evaluation harness spans the eight existing SGACA phases including return risk"
    test_command: "python ai-tier/evals/runner.py"

  ci_pipeline:
    status: TESTED
    evidence: .github/workflows/ci.yml
    description: "CI gates Go tests/races, Python unit/conformance tests, return-risk evals, master evals, and generated-submission drift"

  concurrent_stress_test:
    status: TESTED
    evidence: gateway/worker_test.go
    description: "Concurrent worker stress coverage for durable job processing"
    test_command: "go test -run TestConcurrentSettlement -race"
